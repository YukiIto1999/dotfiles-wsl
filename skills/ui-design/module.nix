_: {
  config.dotfiles.skills.registry."ui-design" = {
    source = ./skill;
    requiresCapabilities = [ ];
    requiresSkills = [
      "browser-operation"
      "repository-research"
    ];
  };
}
